## What's New

- Refined session and app state handling so the macOS client behaves more predictably during longer OpenCode runs.
- Added the new Maple theme to expand the app's built-in visual options.
- Improved transcript loading and placeholder behavior to make conversations feel smoother and less error-prone.

## Improvements

- Reduced memory pressure and improved background process cleanup for better runtime stability.
- Tightened stop-button, theme picker, and shell interaction behavior across the app.
- Cleaned up OpenCode invocation details to better match real project workflows.

## Full Changelog

- Fix state management
- Add Maple theme
- Fix scrollback loading
- Make placeholder not accept clicks; don't explicitly use node to wrap opencode invocation
- Theme picker size fix
- Better background process cleanup
- Improve memory usage; fix stop button behavior
